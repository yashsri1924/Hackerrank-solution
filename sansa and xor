#include <stdio.h>

int sansaXor(int arr_count, int* arr) {
    if (arr_count % 2 == 0) {
        return 0;
    } else {
        int result = 0;
        for (int i = 0; i < arr_count; i += 2) {
            result ^= arr[i];
        }
        return result;
    }
}

